## Summary
What changed and why?

## Scope (check all that apply)
- [ ] Frontend (`src/**`, `public/**`)
- [ ] Backend (`server.js`, `utils/**`, `supabase/**`)
- [ ] Docs (`*.md`, `docs/**`)
- [ ] Integration (`packages/**`, `libs/**`)

## How to Test
- Commands:
npm install
npm run dev
- Steps to verify:
1.
2.
3.

## API (backend PRs)
For each endpoint touched/added:
- **METHOD PATH**:
- **Inputs** (required/optional, types):
- **Responses** (success + common error shapes):
- **Status codes**:
- ENV vars required (names only):

## Screenshots/GIF (frontend PRs)
_Add before/after if UI changed._

## Backwards Compatibility
Any breaking changes? If yes, what needs updating elsewhere?

## TODOs / Follow-ups
- [ ] (tag `area:frontend` or `area:backend` as needed)
